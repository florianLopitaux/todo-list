export default function Header(props) {
    //state

    // actions

    // render
    return (
        <header>
            <h1>Task progression = {props.tasksValidate()} / {props.tasksCount}</h1>
        </header>
    );
}
